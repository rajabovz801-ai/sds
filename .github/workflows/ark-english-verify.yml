name: ark-english-verify

on:
  push:
    branches:
      - main
      - feat/ark-english-telegram-registration
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm install
      - run: npm test
      - run: npm run typecheck
      - run: npm run build
